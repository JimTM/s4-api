import { expect } from 'chai';
import { getStudents } from '../../src/controllers/ClassController';
describe('student controller', function() {
  it('should return an array of students ', function() {});
});
